# Rust
/engine/target/
# Library crate (consumed by other projects) - don't pin transitive
# deps for downstream consumers. Revisit if this becomes a binary/app.
Cargo.lock

# Xcode (future iOS app)
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
xcuserdata/
DerivedData/
*.moved-aside
*.pbxuser
*.mode1v3
*.mode2v3
*.perspectivev3

# Android (future Android app)
.gradle/
local.properties
*.apk
*.aab

# macOS
.DS_Store
